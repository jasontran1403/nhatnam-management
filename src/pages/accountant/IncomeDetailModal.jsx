/**
 * IncomeDetailModal — trước đây là bản sao TỪNG BYTE của ExpenseDetailModal
 * (473 dòng chép nguyên). Giữ hai bản nghĩa là mọi sửa lỗi phải làm hai lần, và
 * thực tế chúng đã bắt đầu lệch nhau.
 *
 * ⚠️ CẦN KIỂM TRA: bản gốc gọi `expenseApi` cho CẢ phiếu thu. Nếu backend có
 * `incomeApi` riêng thì đây là lỗi có sẵn từ trước, không phải do việc gộp file.
 * Khi xác nhận, hãy thêm prop `api` vào ExpenseDetailModal và truyền vào đây.
 */
export { default } from './ExpenseDetailModal';
